---
name: offline-diagnostics-fix
description: Автоматическое исправление проблемы диагностики офлайн‑терминалов (правило `rule_offline` уже корректно обрабатывает `dying-gasp` и формирует рекомендации)
source: auto-skill
extracted_at: '2026-06-27T08:45:12.000Z'
---

## Цель
Обеспечить корректную диагностику терминалов, находящихся в состоянии **offline**, с правильным определением причины `dying-gasp` (отключение питания) и выводом рекомендаций пользователю.

## Шаги решения
1. **Проверка правила `rule_offline`** в `core/engine.py`:
   - Убедиться, что проверка `if not metrics.is_online` правильно отсекает онлайн‑терминалы.
   - Убедиться, что условие `if "dying-gasp" in cause:` существет и возвращает:
     ```python
     DiagnosisProblem(
         "critical",
         "power",
         f"Power loss: {metrics.last_down_cause}",
         "Необходимо проверить электропитание терминала, исправность БП."
     )
     ```
2. **Тестирование** через встроенный smoke‑тест `tests.test_smoke.test_offline_dying_gasp` – он уже проходит и подтверждает, что диагноз формируется корректно.
3. **Обновление шаблона отчёта** в `core/report.py` (функция `to_text`) – уже реализовано отображение причины `dying-gasp` как `Отключение питания`.
4. **Автоматическая валидация**:
   - После любого изменения кода запускать `uv run python -m tests.test_smoke`.
   - При успешном проходе тестов считать задачу выполненной.

## Как применять
- При появлении офлайн‑терминала в отчёте, проверять, что в блоке «Причина» указано `dying-gasp` и в рекомендациях присутствует рекомендация о проверке питания.
- Если причина отличается, добавить соответствующее условие в `rule_offline` согласно шаблону ниже:
  ```python
  if "<новая причина>" in cause:
      return DiagnosisProblem("critical", "<категория>", f"<описание>: {metrics.last_down_cause}", "<рекомендация>")
  ```

## Поддержка
- При необходимости добавить новые причины, использовать тот же подход.
- При изменениях в структуре `OntMetrics` проверять, что поле `last_down_cause` сохраняет оригинальное значение из OLT.

---
**NOTE**: Данная автоматизация использует уже существующее правило и тесты, поэтому дополнительных изменений коду не требуется; достаточно убедиться, что они находятся в актуальном состоянии.
